# Copy to `.env` (next to docker-compose.yml). Used by `docker compose up`.
# `.env` is git-ignored — never commit real keys.
# Leave GEMINI_API_KEY blank to run in deterministic mock mode.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest
